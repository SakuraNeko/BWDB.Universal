<Page
    x:Class="BWDB.Universal.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BWDB.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions >
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource BackgroundAccentBrush}">
            <TextBox Margin="8" Width="300" HorizontalAlignment="Right"/>
        </Grid>
        <Grid Grid.Row="1">
            <SplitView x:Name="SplitView" DisplayMode="Inline" IsPaneOpen="True"  x:FieldModifier="Public">
                <SplitView.Pane>
                    <Frame x:Name="PanelPageFrame" x:FieldModifier="Public"/>
                </SplitView.Pane>
                <SplitView.Content>
                    <Grid>
                        <Grid.ColumnDefinitions >
                            <ColumnDefinition Width=" 300"/>
                            <ColumnDefinition />
                            
                        </Grid.ColumnDefinitions>
                        <Frame x:Name="LeftPageFrame" Grid.Column="0" x:FieldModifier="Public"/>
                        <Frame x:Name="MainPageFrame" Grid.Column="1" x:FieldModifier="Public"/>
                    </Grid>
                    
                </SplitView.Content>
            </SplitView>
        </Grid>
    </Grid>
</Page>
