<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BWDB.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Core="using:BWDB.Core"
    x:Class="BWDB.Universal.BuildPage"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Page.Resources>
        <local:BuildTagStripConverter x:Name="BuildTagStripConverter"/>
        <local:VersionStripConverter x:Name="VersionStripConverter"/>
    </Page.Resources>
    <Grid>

            
       
        <!--StackPanel Width="300" Height="65" Margin="10" Padding="10" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <TextBlock x:Name="TextBlock_Version" Text="10.0.10240.0" Style="{ThemeResource FlyoutPickerTitleTextBlockStyle}"/>
            <TextBlock x:Name="TextBlock_Buildtag" RelativePanel.Below="TextBlock_Version" Text="th1_release" Style="{ThemeResource BodyTextBlockStyle}"/>
        </-->

        <SemanticZoom >
            <SemanticZoom.ZoomedInView >
                <ListView x:Name="ZoomInListView" SelectionChanged="ZoomInListView_SelectionChanged">

                    <ListView.ItemContainerStyle >
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate >
                        <DataTemplate>
                            <StackPanel Width="300" Height="65" Margin="0" Padding="0,10,0,10">
                                <TextBlock x:Name="TextBlock_Version" Text="{Binding Version, Converter={StaticResource VersionStripConverter}}" Style="{ThemeResource FlyoutPickerTitleTextBlockStyle}"/>
                                <TextBlock x:Name="TextBlock_Buildtag" RelativePanel.Below="TextBlock_Version" Text="{Binding Buildtag, Converter={StaticResource BuildTagStripConverter}}" Style="{ThemeResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate >
                                <DataTemplate >
                                    <TextBlock Text="{Binding Key}" Style="{ThemeResource BaseTextBlockStyle}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate >
                        </GroupStyle>
                    </ListView.GroupStyle>
                </ListView>
            </SemanticZoom.ZoomedInView>
            <SemanticZoom.ZoomedOutView >
                <ListView x:Name="ZoomOutListView">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Group.Key}" Style="{ThemeResource BaseTextBlockStyle}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </SemanticZoom.ZoomedOutView>
        </SemanticZoom>
    </Grid>
</Page>
